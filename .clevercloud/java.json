{
  "deploy": [
    "echo '🎯 Deploy hook running...'",
    "pwd && ls -la",
    "mvn clean package -DskipTests"
  ],
  "start": "echo '🚀 Starting App...' && ls target && mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=$PORT"
}
