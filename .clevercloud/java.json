{
  "deploy": [
    "echo '🎯 Deploy hook running...'",
    "mvn clean package -DskipTests"
  ],
  "start": "echo '🚀 Starting App...' && mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=$PORT"
}
